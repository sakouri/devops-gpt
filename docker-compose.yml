version: '3.9'

services:
  chat-api:
    build: ./backend
    ports:
      - "3000:3000"
    depends_on:
      - cache

  chat-ui:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - chat-api
    environment:
      - VITE_API_URL=http://chat-api:3000

  cache:
    image: redis:7-alpine
    ports:
      - "6379:6379"